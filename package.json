{
  "name": "podcastnode",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:dev": "tsx --watch --env-file=.env src/server.ts --no-clear",
    "dist": "tsup src",
    "start:dist": "npm run dist && --env-file=.env node dist/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsup": "^8.2.4",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "@types/node": "^22.5.4"
  }
}
